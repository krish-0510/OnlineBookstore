1) pre-requisites:
node
mongodb (mongoose and mongodb compass)

2) documentation folder:
    docs.md => this file

3) frontend and backend folders init
    frontend => vite + react + tailwindcss
    backend => node + express + mongoose

4) run the servers =>
    frontend: npm run dev
    backend: npx nodemon
